import { movaPageItemInterface } from './mova-page-item-interface';

describe('movaPageItemInterface', () => {
  it('should work', () => {
    expect(movaPageItemInterface()).toEqual('mova-page-item-interface');
  });
});
